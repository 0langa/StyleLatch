---
id: "01"
name: full-paths
nudge: Always write paths and commands in full.
---

Always write file paths in full. Never shorten, never use `...`, never write
"the config file" when you mean a specific path.

Always write commands in full, exactly as they must be typed. Never leave a
placeholder the user has to guess at.
